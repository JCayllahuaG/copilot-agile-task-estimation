spring:
  application:
    name: testing-copilot-agent-test
  r2dbc:
    url: r2dbc:h2:mem:///test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

logging:
  level:
    org.springframework.r2dbc: DEBUG
    io.r2dbc.h2: DEBUG